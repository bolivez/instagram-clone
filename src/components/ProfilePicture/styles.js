import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 75,
    height: 75,
    borderRadius: 75,
    borderWidth: 3,
    borderColor: 'red',
    marginHorizontal: 3,
  },
});

export default styles;
